#include<stdio.h>

int main()
{
    int a1=0, a2=0, b1=0, b2=0, c1=0, c2=0;
    int lasta = -1, lastb = -1, lastc = -1, temp;
    while(scanf("%d", &temp) != EOF){
        if(temp == 1){
            
        }
        if(temp == 2){
            
        }
        if(temp == 3){
            
        }
    }
    return 0;
}